apiVersion: v1
kind: Service
metadata:
  name: service
  namespace: ${CIRCLE_PROJECT_REPONAME}
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: app
  type: NodePort
apiVersion: v1
kind: Service
metadata:
  namespace: ${NAMESPACE}
  name: service
  annotations:
    cloud.google.com/neg: '{"ingress": true}'
spec:
  type: NodePort
  selector:
    app: app
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080